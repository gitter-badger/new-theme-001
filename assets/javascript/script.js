$(document).ready(function() {
    /*
     * init browser
     */
    init();

    /*
     * init resize browser
     */
    $(window).resize(function() {
        init();
    });

});

/*
 * init function
 */

function init() {
	// Code init function
}